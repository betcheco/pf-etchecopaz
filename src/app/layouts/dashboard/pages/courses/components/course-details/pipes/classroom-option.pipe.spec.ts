import { ClassroomOptionPipe } from './classroom-option.pipe';

describe('ClassroomOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassroomOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
